# Output Assets

This directory is reserved for stable templates or reusable output assets.
The current Skill generates Markdown, CSV and JSON directly with `scripts/build_outputs.py`, so no binary template is required.
